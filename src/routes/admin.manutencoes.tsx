import { createFileRoute, Link } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { AdminTopbar } from "@/components/admin/AdminTopbar";
import { ActionDialog } from "@/components/admin/ActionDialog";
import { FilterBar, TableShell } from "@/components/admin/AdminBlocks";
import { StatCard } from "@/components/StatCard";
import { StatusBadge } from "@/components/StatusBadge";
import { maintenances, vehicles, mechanics, services } from "@/lib/mock-data";
import { brl, formatDate, num, sum } from "@/lib/calculations";
import { maintenanceStatusLabel, maintenanceStatusTone, maintenanceTypeLabel, serviceCategoryLabel } from "@/lib/status-rules";
import type { MaintenanceStatus, MaintenanceType } from "@/types/fleet";

export const Route = createFileRoute("/admin/manutencoes")({ head: () => ({ meta: [{ title: "Manutenções — Admin" }] }), component: MaintenancesPage });
function MaintenancesPage() {
  const [q, setQ] = useState(""); const [status, setStatus] = useState<MaintenanceStatus | "all">("all"); const [type, setType] = useState<MaintenanceType | "all">("all"); const [vehicleId, setVehicleId] = useState("all");
  const filtered = useMemo(() => maintenances.filter((m) => { const v = vehicles.find((x) => x.id === m.vehicleId); const ms = m.serviceIds.map((id) => services.find((s) => s.id === id)?.name).join(" "); const text = `${v?.plate ?? ""} ${v?.model ?? ""} ${m.cause} ${ms}`.toLowerCase(); return text.includes(q.toLowerCase()) && (status === "all" || m.status === status) && (type === "all" || m.maintenanceType === type) && (vehicleId === "all" || m.vehicleId === vehicleId); }), [q, status, type, vehicleId]);
  const total = sum(filtered.map((m) => m.value));
  return <><AdminTopbar title="Manutenções" subtitle="Ordens preventivas e corretivas com um ou mais serviços realizados" actions={<ActionDialog triggerLabel="+ Nova manutenção" title="Nova manutenção" description="Abra uma ordem preventiva ou corretiva e vincule os serviços realizados." submitLabel="Salvar manutenção" fields={[{ label: "Veículo", type: "select", options: vehicles.map((v) => ({ label: `${v.plate} · ${v.model}`, value: v.id })) }, { label: "Tipo", type: "select", options: Object.entries(maintenanceTypeLabel).map(([value, label]) => ({ label, value })) }, { label: "Serviços", type: "checkboxes", options: services.map((s) => ({ label: s.name, value: s.id })) }, { label: "Causa / descrição", type: "textarea", wide: true }, { label: "Data", type: "date", value: "2026-05-29" }, { label: "KM atual", type: "number" }, { label: "Mecânico", type: "select", options: mechanics.map((m) => ({ label: m.name, value: m.id })) }, { label: "Valor", type: "number" }, { label: "Status", type: "select", options: Object.entries(maintenanceStatusLabel).map(([value, label]) => ({ label, value })) }, { label: "Observações", type: "textarea", wide: true }]} />} /><div className="space-y-4 p-6"><div className="grid gap-3 md:grid-cols-4"><StatCard label="Registros" value={filtered.length} /><StatCard label="Preventivas" value={filtered.filter((m) => m.maintenanceType === "preventiva").length} tone="ok" /><StatCard label="Corretivas" value={filtered.filter((m) => m.maintenanceType === "corretiva").length} tone="danger" /><StatCard label="Valor" value={brl(total)} /></div><FilterBar><input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Buscar placa, serviço ou causa..." className="rounded-md border border-input bg-background px-3 py-2 text-sm" /><select value={status} onChange={(e) => setStatus(e.target.value as MaintenanceStatus | "all")} className="rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="all">Todos os status</option>{Object.entries(maintenanceStatusLabel).map(([k, v]) => <option key={k} value={k}>{v}</option>)}</select><select value={type} onChange={(e) => setType(e.target.value as MaintenanceType | "all")} className="rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="all">Todos os tipos</option>{Object.entries(maintenanceTypeLabel).map(([k, v]) => <option key={k} value={k}>{v}</option>)}</select><select value={vehicleId} onChange={(e) => setVehicleId(e.target.value)} className="rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="all">Todos os veículos</option>{vehicles.map((v) => <option key={v.id} value={v.id}>{v.plate} · {v.model}</option>)}</select></FilterBar><TableShell head={["Data", "Veículo", "Tipo", "Serviços", "Causa", "Mecânico", "KM", "Valor", "Status", ""]}>{filtered.map((m) => { const v = vehicles.find((x) => x.id === m.vehicleId); const me = mechanics.find((x) => x.id === m.mechanicId); const sv = m.serviceIds.map((id) => services.find((s) => s.id === id)).filter(Boolean); return <tr key={m.id} className="hover:bg-muted/40"><td className="px-4 py-3">{formatDate(m.date)}</td><td className="px-4 py-3 font-semibold">{v?.plate}<div className="text-xs font-normal text-muted-foreground">{v?.brand} {v?.model}</div></td><td className="px-4 py-3">{maintenanceTypeLabel[m.maintenanceType]}</td><td className="px-4 py-3"><div className="flex flex-wrap gap-1">{sv.slice(0, 3).map((s) => <span key={s!.id} className="rounded bg-muted px-2 py-1 text-xs">{s!.name}</span>)}{sv.length > 3 && <span className="text-xs text-muted-foreground">+{sv.length - 3}</span>}</div><div className="mt-1 text-xs text-muted-foreground">{sv[0] ? serviceCategoryLabel[sv[0].category] : "—"}</div></td><td className="px-4 py-3 text-muted-foreground">{m.cause}</td><td className="px-4 py-3">{me?.name ?? "—"}</td><td className="px-4 py-3 text-right">{num(m.currentKm)}</td><td className="px-4 py-3 text-right">{brl(m.value)}</td><td className="px-4 py-3"><StatusBadge tone={maintenanceStatusTone[m.status]}>{maintenanceStatusLabel[m.status]}</StatusBadge></td><td className="px-4 py-3 text-right"><Link to="/admin/manutencoes/$id" params={{ id: m.id }} className="font-medium text-primary hover:underline">Detalhes →</Link></td></tr>; })}</TableShell></div></>;
}
