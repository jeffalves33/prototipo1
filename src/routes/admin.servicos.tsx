import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { AdminTopbar } from "@/components/admin/AdminTopbar";
import { FilterBar, DetailRow, SectionCard, TableShell } from "@/components/admin/AdminBlocks";
import { StatCard } from "@/components/StatCard";
import { StatusBadge } from "@/components/StatusBadge";
import { services, maintenances, pendencies } from "@/lib/mock-data";
import { maintenanceTypeLabel, periodicityLabel, pendencySeverityLabel, pendencySeverityTone, serviceCategoryLabel } from "@/lib/status-rules";
import type { PeriodicityType, ServiceCategory } from "@/types/fleet";

export const Route = createFileRoute("/admin/servicos")({ head: () => ({ meta: [{ title: "Serviços — Admin" }] }), component: ServicesPage });
function ServicesPage() {
  const [q, setQ] = useState(""); const [cat, setCat] = useState<ServiceCategory | "all">("all"); const [periodicity, setPeriodicity] = useState<PeriodicityType | "all">("all"); const [selected, setSelected] = useState(services[0].id);
  const filtered = useMemo(() => services.filter((s) => `${s.name} ${s.description}`.toLowerCase().includes(q.toLowerCase()) && (cat === "all" || s.category === cat) && (periodicity === "all" || s.periodicityType === periodicity)), [q, cat, periodicity]);
  const service = services.find((s) => s.id === selected) ?? filtered[0] ?? services[0];
  const useCount = service ? maintenances.filter((m) => m.serviceIds.includes(service.id)).length : 0;
  const servicePend = service ? pendencies.filter((p) => p.serviceId === service.id) : [];
  return <><AdminTopbar title="Serviços" subtitle="Cadastro de serviços periódicos. Óleo e pneus são categorias de serviço, não módulos separados." actions={<button className="rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-foreground">+ Novo serviço</button>} /><div className="space-y-4 p-6"><div className="grid gap-3 md:grid-cols-4"><StatCard label="Serviços" value={services.length} /><StatCard label="Por KM" value={services.filter((s) => s.periodicityType === "km").length} /><StatCard label="Por tempo" value={services.filter((s) => s.periodicityType === "time").length} /><StatCard label="Sem recorrência" value={services.filter((s) => s.periodicityType === "none").length} /></div><FilterBar><input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Buscar serviço..." className="rounded-md border border-input bg-background px-3 py-2 text-sm" /><select value={cat} onChange={(e) => setCat(e.target.value as ServiceCategory | "all")} className="rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="all">Todas categorias</option>{Object.entries(serviceCategoryLabel).map(([k, v]) => <option key={k} value={k}>{v}</option>)}</select><select value={periodicity} onChange={(e) => setPeriodicity(e.target.value as PeriodicityType | "all")} className="rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="all">Todas periodicidades</option><option value="km">Por KM</option><option value="time">Por tempo</option><option value="none">Sem recorrência</option></select><div className="rounded-md border border-input bg-background px-3 py-2 text-sm text-muted-foreground">Regra: KM ou tempo, nunca ambos</div></FilterBar><div className="grid gap-4 xl:grid-cols-[1fr_360px]"><TableShell head={["Serviço", "Categoria", "Tipo sugerido", "Periodicidade", "Uso", "Status"]}>{filtered.map((s) => <tr key={s.id} onClick={() => setSelected(s.id)} className={`cursor-pointer hover:bg-muted/40 ${selected === s.id ? "bg-primary/5" : ""}`}><td className="px-4 py-3 font-medium">{s.name}<div className="text-xs text-muted-foreground">{s.description}</div></td><td className="px-4 py-3">{serviceCategoryLabel[s.category]}</td><td className="px-4 py-3">{maintenanceTypeLabel[s.suggestedMaintenanceType]}</td><td className="px-4 py-3">{periodicityLabel(s.periodicityType, s.periodicityKm, s.periodicityDays)}</td><td className="px-4 py-3 text-right">{maintenances.filter((m) => m.serviceIds.includes(s.id)).length}</td><td className="px-4 py-3"><StatusBadge tone={s.status === "ativo" ? "ok" : "muted"}>{s.status}</StatusBadge></td></tr>)}</TableShell><SectionCard title="Detalhe do serviço" subtitle="Modelagem correta do item periódico">{service && <><DetailRow label="Nome" value={service.name} /><DetailRow label="Categoria" value={serviceCategoryLabel[service.category]} /><DetailRow label="Tipo sugerido" value={maintenanceTypeLabel[service.suggestedMaintenanceType]} /><DetailRow label="Periodicidade" value={periodicityLabel(service.periodicityType, service.periodicityKm, service.periodicityDays)} /><DetailRow label="Regra" value={service.periodicityType === "km" ? "Controla por quilometragem" : service.periodicityType === "time" ? "Controla por data" : "Sem recorrência"} /><DetailRow label="Usos em manutenção" value={useCount} /><DetailRow label="Pendências ligadas" value={servicePend.length} /><div className="mt-4 space-y-2">{servicePend.map((p) => <div key={p.id} className="rounded-md border border-border p-2 text-sm"><div className="flex justify-between gap-2"><span>{p.title}</span><StatusBadge tone={pendencySeverityTone[p.severity]}>{pendencySeverityLabel[p.severity]}</StatusBadge></div><p className="mt-1 text-xs text-muted-foreground">{p.description}</p></div>)}</div></>}</SectionCard></div></div></>;
}
